<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inscription</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <style>
    body {
      background: url('/images/backend.png') no-repeat center center/cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .form-container {
      background: rgba(255, 255, 255, 0.5);
      backdrop-filter: blur(5px);
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
      max-width: 400px;
      width: 100%;
    }

    .text-link {
      color: #2d6a4f;
      text-decoration: none;
      position: relative;
      z-index: 1;
    }

    .form-title {
      text-align: center;
      color: #2d6a4f;
      font-weight: bold;
    }

    .form-control {
      border-radius: 10px;
      padding-left: 40px;
    }

    .input-group-text {
      background: transparent;
      border: none;
      position: absolute;
      left: 10px;
      color: #2d6a4f;
    }

    .btn-green {
      background: linear-gradient(45deg, #2d6a4f, #1b4332);
      color: white;
      border: none;
      transition: transform 0.2s ease-in-out;
    }

    .btn-green:hover {
      transform: scale(1.05);
      background: linear-gradient(45deg, #1b4332, #081c15);
    }

    .form-control:focus {
      border: 2px solid #2d6a4f;
      box-shadow: 0 0 8px #2d6a4f;
    }

    .text-link:hover {
      text-decoration: underline;
    }

    .header {
      position: absolute;
      top: 0;
      width: 100%;
      background: rgba(255, 255, 255, 0.7);
      padding: 10px 0;
    }

    .nav-container {
      display: flex;
      justify-content: center;
    }

    nav ul {
      list-style: none;
      padding: 0;
      display: flex;
      gap: 15px;
    }

    nav ul li {
      position: relative;
    }

    nav ul li a {
      text-decoration: none;
      color: #2d6a4f;
      font-weight: bold;
      padding: 10px;
      display: block;
    }

    .dropdown-menu {
      display: none;
      position: absolute;
      top: 100%;
      left: 0;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 5px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
      list-style: none;
      padding: 10px 0;
      min-width: 150px;
    }

    .dropdown:hover .dropdown-menu {
      display: block;
    }

    .dropdown-menu li a {
      padding: 10px;
      display: block;
      color: #2d6a4f;
      text-decoration: none;
    }

    .dropdown-menu li a:hover {
      background: #2d6a4f;
      color: white;
    }
  </style>
</head>
<body>
  <header class="header nav-container">
    <nav>
      <ul>
        <li><a href="{{ path('app_home') }}">Accueil</a></li>
        <li class="dropdown">
          <a href="#">Menu ▾</a>
          <ul class="dropdown-menu">
            <li><a href="{{ path('app_produits') }}">Produits</a></li>
            <li><a href="{{ path('app_associations') }}">Associations</a></li>
            <li><a href="{{ path('app_evenements') }}">Événements</a></li>
            <li><a href="{{ path('app_commandes') }}">Commandes</a></li>
            <li><a href="{{ path('app_sponsors') }}">Sponsors</a></li>
            <li><a href="{{ path('app_dons') }}">Dons</a></li>
            <li><a href="{{ path('app_formations') }}">Formations</a></li>
            <li><a href="{{ path('app_modules') }}">Modules</a></li>
          </ul>
        </li>
      </ul>
    </nav>
  </header>
  {{ include('includes/flashes.twig') }}
  <div class="form-container">
    <h3 class="form-title">Inscription</h3>
    {{ form_start(form) }}
      <div class="mb-3 position-relative">
          <label for="form_fullName" class="form-label mb-2 fs-13 label-color font-w500">Name</label>
          <div class="input-group">
              <span class="input-group-text"><i class="fas fa-user"></i></span>
              {{ form_widget(form.fullName, {'attr': {'class': 'form-control'}}) }}
          </div>
      </div>
      <div class="mb-3 position-relative">
       <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Email</label>
        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
        {{ form_widget(form.email) }}
        {% if form_errors(form.email) is not empty %}
          <div class="alert-custom alert-danger">{{ form_errors(form.email) }}</div>
       {% endif %}
      </div>
      <div class="mb-3 position-relative">
       <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Roles</label>
        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
        {{ form_widget(form.roles) }}
        {% if form_errors(form.roles) is not empty %}
          <div class="alert-custom alert-danger">{{ form_errors(form.roles) }}</div>
       {% endif %}
      </div>
      <div class="mb-3 position-relative" id="verifedForm" style= "display: none">
       <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Verification Formateur ID</label>
        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
        {{ form_widget(form.verifedForm) }}
        {% if form_errors(form.verifedForm) is not empty %}
          <div class="alert-custom alert-danger">{{ form_errors(form.verifedForm) }}</div>
       {% endif %}
      </div>
      <div class="mb-3 position-relative" id="verifedEntreprise" style= "display: none">
       <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Verification Entreprise ID</label>
        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
        {{ form_widget(form.verifedEntreprise) }}
        {% if form_errors(form.verifedEntreprise) is not empty %}
          <div class="alert-custom alert-danger">{{ form_errors(form.verifedEntreprise) }}</div>
       {% endif %}
      </div>
      <div class="mb-3 position-relative" id="verifedAssoc" style= "display: none">
       <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Verification Association ID</label>
        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
        {{ form_widget(form.verifedAssoc) }}
        {% if form_errors(form.verifedAssoc) is not empty %}
          <div class="alert-custom alert-danger">{{ form_errors(form.verifedAssoc) }}</div>
       {% endif %}
      </div>
      <div class="mb-3 position-relative" id="verifedDonne" style= "display: none">
       <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Verification Donneur ID</label>
        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
        {{ form_widget(form.verifedDonne) }}
        {% if form_errors(form.verifedDonne) is not empty %}
          <div class="alert-custom alert-danger">{{ form_errors(form.verifedDonne) }}</div>
       {% endif %}
      </div>
      <div class="mb-3 position-relative">
      <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Password</label>
        <span class="input-group-text"><i class="fas fa-lock"></i></span>
         {{ form_widget(form.password.first)}}
           {% if form_errors(form.password) is not empty %}
                <div class="alert-custom alert-danger">{{ form_errors(form.password)}}</div>
           {% endif %}
      </div>
       <div class="mb-3 position-relative">
       <label for="exampleFormControlInput1" class="form-label mb-2 fs-13 label-color font-w500">Confirmer Password</label>
        <span class="input-group-text"><i class="fas fa-lock"></i></span>
         {{ form_widget(form.password.second)}}
         {% if form_errors(form.password) is not empty %}
               <div class="alert-custom alert-danger">{{ form_errors(form.password)}}</div>
         {% endif %}
      </div>
    <div class="mb-3 position-relative">
        {{ form_row(form.recaptcha) }}
    </div>

      <button type="submit" class="btn btn-green w-100">S'inscrire</button>
   {{ form_end(form) }}
    <p class="text-center mt-3">
      Vous avez déjà un compte ? <a href="{{ path('app_login') }}" class="text-link">Connectez-vous ici</a>
    </p>
  </div>
</body>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
     <script src="{{ asset('assets/js/crud.js') }}"></script>
     <script src="https://www.google.com/recaptcha/api.js" async defer></script>

</html>