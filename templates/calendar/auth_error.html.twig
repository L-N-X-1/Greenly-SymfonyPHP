{% extends 'base.html.twig' %}

{% block title %}Google Calendar Authentication Error{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="alert alert-danger">
        <h4 class="alert-heading">Authentication Error</h4>
        <p>{{ error }}</p>
        <hr>
        <p class="mb-0">
            <a href="{{ path('app_event_index') }}" class="btn btn-primary">Return to Events</a>
        </p>
    </div>
</div>
{% endblock %}
